  // js is also a functional language -- when you have a higher order function ex: a function that takes a function as a parameter -- higher order functions are synchronous //

  // example code //

  var numbers = [1, 4, 9];

  var roots = numbers.map(Math.sqrt);

  console.log("Numbers = " + numbers);
  console.log("Roots = " + roots);
